# application_pet.R-CNN_resnet
webアプリとして必要最低限の内容で構成されるリポジトリ。 第三者に見られる想定でコード内容を精査

---

pet_appフォルダを第三者に提出する場合の注意点:
* .gitignoreファイルはgitを用いる上で使用されるファイルなので、提出時には含まないこと
* 必ずディレクトリ全体をzipファイルなどに圧縮すること
* このREADMEファイルから業務連絡部分を削除し、pet_appフォルダと一緒に提出すること(アプリケーション実行方法の説明になります)


以下実行方法の説明。この行含む上の文は、業務連絡のため提出時は削除すること
---
【システム起動方法】

これは、FastAPIとDockerで構築されたWebアプリケーションです。

前提条件:
Docker Desktopがインストールされている

1. pet_app ディレクトリをダウンロードし、pet_app内へ移動する。
2. Docker 実行環境が整っている環境で docker build -t pet_app . を実行する。
3. docker run -p 8000:8000 pet_app を実行する。
4. ブラウザで以下のURLにアクセスしてください。: http://127.0.0.1:8000/static/pet.html

※注意事項
・手順2、3は時間がかかる場合があります。
